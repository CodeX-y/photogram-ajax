console.log("Create!!!")

var follow = $(".<%= dom_id(@follow_request.sender) %>_<%= dom_id(@follow_request.recipient) %>");

var following = $("<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient)%>");

follow.replaceWith(following);
