console.log("Destroy!!!")

var following = $(".<%= dom_id(@follow_request.sender) %>_<%= dom_id(@follow_request.recipient) %>")

var unfollow = $("<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient) %>")

following.replaceWith(unfollow)
